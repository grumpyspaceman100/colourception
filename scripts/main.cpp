function main () {
wall → show back button(false)
wall → set background(colors → black)
data → board := media → create landscape board(1080, 1920)
data → board → set background(colors → black)
code → circleAnimation
data → board → post to wall
}
